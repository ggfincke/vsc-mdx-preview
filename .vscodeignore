# VS Code local settings
.vscode/**
.vscode-test/**

# Build artifacts to exclude (only extension.js bundle & webview-app are needed)
build/extension/test/**
build/**/*.map
build/extension/extension-host/**
build/extension/contracts/**
build/extension/runtime-utils/**
build/extension/codegen/**
build/extension/meta.json
build/.tsbuildinfo
build/webview-app/.vite/**
!build/webview-app/.vite/manifest.json

# Stale assets
assets/old_icon.png

# README embeds the GitHub raw URL for this - no need to ship it
assets/example.gif

# Source code (not needed - bundled into extension.js)
packages/**

# Development documentation
dev-docs/**
CLAUDE.md
AGENTS.md

# Test files
tests/**
coverage/**

# Examples & docs (README links are absolute GitHub URLs)
examples/**
docs/**

# Private packages
node_modules/webview/**
node_modules/webview
node_modules/@mdx-preview/**

# Node dependencies (bundled into extension.js)
node_modules/**

# Exclude mdx-forge source tree (installed as npm dep; symlinked locally via npm link)
# vsce follows symlinks, which would include the entire sibling directory otherwise
../mdx-forge/**

# Configuration files
.gitignore
.prettierrc*
.prettierignore
tsconfig.json
tsconfig.*.json
vitest.config.ts
tests/vitest.config.ts
eslint.config.mjs
esbuild.config.mjs
package-lock.json

# Claude/AI configuration
.agents/**
.claude/**

# Scripts (development only)
scripts/**

# GitHub/CI configuration (if exists)
.github/**

# Editor configs
.editorconfig

# NPM/Yarn
.npmrc
.yarnrc*
yarn.lock

# Old package files
*.vsix
